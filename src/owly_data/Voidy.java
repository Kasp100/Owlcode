package owly_data;

import owlcode.Type;

/** If a function or method does not return anything, an OwlyVoid object will be returned. */
public class Voidy extends Type {

	public Voidy(boolean isPrimitive) {
		super(true);
	}};
